# Jump Analyzer 設定ファイル
# このファイルを config.toml にコピーして使用してください
# コマンドライン引数は設定ファイルの値を上書きします

# 入力・出力パス
filename = "nakamura_horizon2"  # 入力ファイル名（.bagは不要）
    # filenameを指定すると自動的にパスが生成される:
    #   input = "record/{filename}.bag"
    #   output = "results/{filename}"

# input/outputを個別に指定する場合はfilenameをコメントアウトして以下を使用:
# input = "record/custom.bag"
# output = "results/custom"

dual = false  # デュアルカメラモード（trueの場合、inputにディレクトリを指定すると自動で*_metadata.jsonを検出）

# モデル設定
model_dir = "models"  # YOLOv8-Poseモデルのディレクトリ
model_name = "yolov8x-pose.pt"  # モデル名 (yolov8n-pose.pt, yolov8s-pose.pt, yolov8m-pose.pt, yolov8l-pose.pt, yolov8x-pose.pt)

# ジャンプ検出閾値
threshold_vertical = 0.05  # 垂直ジャンプ検出閾値（メートル）
threshold_horizontal = 0.1  # 水平ジャンプ検出閾値（メートル）
min_jump_height = 0.10  # 有効なジャンプと認識する最小高さ（メートル、デフォルト: 10cm）
min_air_time = 0.20  # 有効なジャンプと認識する最小滞空時間（秒、デフォルト: 200ms）

# 出力オプション
enable_video = true  # ビデオ出力を有効化（true: 出力する、false: スキップ）
enable_3d_animation = true  # 3Dアニメーションを有効化（true: 生成する、false: スキップ）
interactive_3d = true  # インタラクティブ3Dアニメーションを表示
video_fps = 60  # 出力動画のFPS（元の録画FPSに合わせる。30または60が一般的）

# キーポイントスムージング
smooth_keypoints = true  # キーポイントスムージングを有効化（falseの場合は無効、trueの場合はsmooth_window_sizeを使用）
smooth_window_size = 5  # 移動平均のウィンドウサイズ（smooth_keypoints: trueの場合に使用）

# 深度データ処理
enable_depth_interpolation = true  # 深度補間を有効化（true: 補間する、false: 単一ピクセルを使用）
depth_kernel_size = 3  # 深度補間カーネルサイズ（3, 5, 7など）

# カルマンフィルタ（高度なスムージング）
use_kalman_filter = true  # カルマンフィルタを使用（smooth_keypointsより高精度だが遅い）
kalman_process_noise = 0.03  # カルマンフィルタ処理ノイズ（小さいほど滑らか、大きいほど反応が速い）
kalman_measurement_noise = 0.1  # カルマンフィルタ測定ノイズ（大きいほど予測を信頼）

# 床検出（高精度なジャンプ検出）
# 注意: 体育館の日光反射などで深度データが破綻している場合は false に設定
enable_floor_detection = false  # 床検出を無効化（従来の高さベース検出を使用）

# 再生時間範囲（秒）
# 0の場合は最初から/最後まで（nullは使えないので0で代用）
start_time = 5.0  # 開始時間（秒、0の場合は最初から）
end_time = 51.0    # 終了時間（秒、0の場合は最後まで）

# start_time = 43.0  # 開始時間（秒、0の場合は最初から）
# end_time = 51.0    # 終了時間（秒、0の場合は最後まで）

# 高速化オプション
frame_skip = 1  # Nフレームおきに処理（1=全フレーム、2=2フレームおき、など）
resize_factor = 1.0  # 画像リサイズ係数（1.0=リサイズなし、0.5=半分のサイズ）
minimal_data = false  # JSONにジャンプ検出フレームのみ保存（高速化、ファイルサイズ削減）

